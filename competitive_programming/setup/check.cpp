#include <iostream>
#include <algorithm>
#include <cmath>

using namespace std;

signed main() {
    freopen("input.txt", "r", stdin);


    freopen("output.txt", "r", stdin);


    if (true) {
        return 0;
    }
    else {
        return 1;
    }
}
